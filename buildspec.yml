version: 0.1
phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - cd lambdas/user
      - npm install
  pre_build:
    commands:
      - npm run lint
      - npm run test
  build:
    commands:
      - npm run deploy
