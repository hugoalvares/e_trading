version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - cd lambdas/user
      - npm install
  pre_build:
    commands:
      - npm run lint
      - npm run test
  build:
    commands:
      - npm run deploy
